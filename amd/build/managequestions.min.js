/**
 * AMD module for mod_playerpuzzle manage-questions page interactions.
 *
 * Provides select-all, bulk approve, bulk delete and single-row delete via a shared
 * bulk form. Approve and delete buttons track separate counts: the approve button
 * counts only pending (unapproved) checked rows; the delete button counts all checked
 * rows. The individual "Delete" button pre-selects its own checkbox then triggers a
 * confirmation modal before submitting. Single-row "Approve" stays a plain link (no
 * confirmation needed — approving is non-destructive), so it is not handled here.
 *
 * @module     mod_playerpuzzle/managequestions
 * @copyright  2026 Jean Lúcio
 * @license    https://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("mod_playerpuzzle/managequestions",["core/modal_save_cancel","core/modal_events","core/str"],(function(ModalSaveCancel,ModalEvents,Str){let bulkForm=null,bulkActionField=null,selectAllCheckbox=null,bulkDeleteBtn=null,bulkApproveBtn=null;const updateBulkButtons=()=>{const totalCount=document.querySelectorAll(".playerpuzzle-bulk-check:checked").length,approvableCount=document.querySelectorAll('.playerpuzzle-bulk-check:checked[data-approvable="1"]').length;bulkDeleteBtn&&(bulkDeleteBtn.disabled=0===totalCount,bulkDeleteBtn.textContent="".concat(bulkDeleteBtn.dataset.labelbase," (").concat(totalCount,")")),bulkApproveBtn&&(bulkApproveBtn.disabled=0===approvableCount,bulkApproveBtn.textContent="".concat(bulkApproveBtn.dataset.labelbase," (").concat(approvableCount,")"))},showModal=async(title,body,onConfirm)=>{try{const[modal,yesStr]=await Promise.all([ModalSaveCancel.create({title:title,body:body,removeOnClose:!0}),Str.get_string("yes","core")]);modal.setSaveButtonText(yesStr),modal.getRoot().on(ModalEvents.save,(()=>onConfirm())),modal.show()}catch(error){window.console.error(error)}};return{init:function(){selectAllCheckbox=document.getElementById("playerpuzzle-select-all"),selectAllCheckbox&&(selectAllCheckbox.addEventListener("change",(()=>{document.querySelectorAll(".playerpuzzle-bulk-check").forEach((cb=>{cb.checked=selectAllCheckbox.checked})),updateBulkButtons()})),document.querySelectorAll(".playerpuzzle-bulk-check").forEach((cb=>{cb.addEventListener("change",(()=>{const total=document.querySelectorAll(".playerpuzzle-bulk-check").length,checked=document.querySelectorAll(".playerpuzzle-bulk-check:checked").length;selectAllCheckbox.checked=checked===total,selectAllCheckbox.indeterminate=checked>0&&checked<total,updateBulkButtons()}))}))),bulkForm=document.getElementById("playerpuzzle-bulk-form"),bulkActionField=document.getElementById("playerpuzzle-bulk-action"),bulkDeleteBtn=document.getElementById("playerpuzzle-bulk-delete-btn"),bulkApproveBtn=document.getElementById("playerpuzzle-bulk-approve-btn"),bulkForm&&(bulkDeleteBtn&&bulkDeleteBtn.addEventListener("click",(()=>{showModal(bulkDeleteBtn.dataset.title,bulkDeleteBtn.dataset.confirm,(()=>{bulkActionField&&(bulkActionField.value="delete"),bulkForm.submit()}))})),bulkApproveBtn&&bulkApproveBtn.addEventListener("click",(()=>{showModal(bulkApproveBtn.dataset.title,bulkApproveBtn.dataset.confirm,(()=>{bulkActionField&&(bulkActionField.value="approve"),bulkForm.submit()}))}))),document.querySelectorAll(".playerpuzzle-single-delete-btn").forEach((btn=>{btn.addEventListener("click",(()=>{const questionid=btn.dataset.questionid;document.querySelectorAll(".playerpuzzle-bulk-check").forEach((cb=>{cb.checked=cb.value===questionid})),selectAllCheckbox&&(selectAllCheckbox.checked=!1,selectAllCheckbox.indeterminate=!1),showModal(btn.dataset.title,btn.dataset.confirm,(()=>{bulkActionField&&(bulkActionField.value="delete"),bulkForm.submit()}))}))})),updateBulkButtons()}}}));

//# sourceMappingURL=managequestions.min.js.map